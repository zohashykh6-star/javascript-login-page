datasource db {
  provider = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model TestEntity {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
}
